@model BookingViewModel
@inject IUtility _Utility

<div class="row page-titles mx-0">
    <div class="col p-md-0">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="javascript:void(0)">Dashboard</a></li>
            <li class="breadcrumb-item active"><a href="javascript:void(0)">Home</a></li>
        </ol>
    </div>
</div>

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8 col-sm-12">
            <div class="card shadow-lg">
                <div class="card-body">
                    <h5 class="card-title text-center">Bookingr</h5>

                    <form asp-action="Booking">
                        <input asp-for="Id" type="hidden" />

                        <div class="row">
                            <!-- Name Field -->
                            <div class="col-lg-12">

                                <div class="form-group">
                                    <label asp-for="VehicleId" class="control-label"></label>
                                    <select asp-for="VehicleId" asp-items=" await _Utility.GetVehicles()" class="form-control">
                                        <option>--	<label asp-for="VehicleId" class="control-label"></label>--</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label asp-for="RenterId" class="control-label"></label>
                                    <select asp-for="RenterId" asp-items=" await _Utility.GetVehicleRenters()" class="form-control">
                                        <option>--	<label asp-for="RenterId" class="control-label"></label>--</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label asp-for="StartDate" class="control-label font-weight-bold"></label>
                                    <input asp-for="StartDate" type="date" class="form-control" placeholder="Enter Owner name" required />
                                    <span asp-validation-for="StartDate" class="text-danger"></span>
                                </div>
                                <div class="form-group">
                                    <label asp-for="EndDate" class="control-label font-weight-bold"></label>
                                    <input asp-for="EndDate" type="date" class="form-control" placeholder="Enter Address" required />
                                    <span asp-validation-for="EndDate" class="text-danger"></span>
                                </div>    
                                
                            </div>
                        </div>

                        <!-- Form Buttons -->
                        <div class="d-flex justify-content-between mt-3">
                            <a asp-action="CategoryIndex" class="btn btn-warning">
                                <i class="fa fa-angle-left"></i> Back
                            </a>
                            <button type="submit" class="btn btn-success">
                                <i class="fa fa-save"></i> Create
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
        <partial name="_ValidationScriptsPartial" />
}
